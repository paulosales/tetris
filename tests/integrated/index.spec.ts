import "../../src/index";

describe("Index", () => {
  describe("when the index bootstraps the game", () => {
    it("should not raise errors", () => {
      expect(true).toBe(true);
    });
  });
});
